# Used by the `npm run migrate` command only — it runs locally from your laptop
# against the Neon database, not inside the Worker.
#
# Copy to .env and fill in your Neon connection string.
DATABASE_URL=postgresql://user:pass@ep-xxx.neon.tech/investment_game?sslmode=require
